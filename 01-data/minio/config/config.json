{
    "version": "10",
    "aliases": {
        "myminio": {
            "url": "http://minio:9000",
            "accessKey": "${MINIO_ROOT_USER}",
            "secretKey": "${MINIO_ROOT_PASSWORD}",
            "api": "s3v4",
            "path": "auto"
        }
    }
}